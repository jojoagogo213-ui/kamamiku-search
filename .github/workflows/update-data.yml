name: 更新データベース（木曜正午）
on:
  schedule:
    - cron: '0 12 * * 4'
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: 📁 ファイル確認
      run: |
        echo "リポジトリ直下: $(ls -la comments_mokuji.csv || echo '❌ CSVなし')"
        echo "scripts/: $(ls scripts/)"
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm install
    - run: node scripts/convert-csv-to-json.js
    - name: ✅ 結果確認
      run: |
        ls -la public/data/comments.json
        head -n 5 public/data/comments.json
    - run: |
        git config user.name "GitHub Action"
        git config user.email "action@github.com"
        git add public/data/comments.json
        if git diff --staged --quiet; then
          echo "変更なし"
        else
          git commit -m "📊 データ更新 $(date +%Y-%m-%d)"
          git push
        fi
