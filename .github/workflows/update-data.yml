name: 更新データベース（木曜正午）

on:
  schedule:
    - cron: '0 12 * * 4'  # 毎週木曜 正午（UTC3時=JST12時）
  workflow_dispatch:  # 手動実行

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Node.jsセットアップ
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - name: 依存関係インストール
      run: npm install
    - name: CSV→JSON変換
      run: node scripts/convert-csv-to-json.js
    - name: コミット＆プッシュ
      run: |
        git config user.name "GitHub Action"
        git config user.email "action@github.com"
        git add public/data/comments.json
        git commit -m "📊 $(date +%Y-%m-%d) データ更新" || exit 0
        git push
